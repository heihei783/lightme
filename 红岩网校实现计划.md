# LightMe 红岩网校考核实现计划

## 1. 目标

将 LightMe 改造为满足红岩网校“可靠 AI Agent 系统开发”考核要求的项目：

- 公共主线：完成可运行、可追踪、可测试、可扩展的 Agent Runtime。
- 专项方向：选择 Planner-Executor 与任务规划。
- 交付材料：代码仓库、自动评测、对照实验、Trace 示例、失败恢复案例和最终 PDF 文档素材。

## 2. 当前项目基础

LightMe 已具备以下能力：

- LangGraph 主控图，支持 `chat`、`rag`、`agent` 三类路由。
- Agent 图包含 Planning、Skill Select、Executor、Reflection、Finalize 等节点。
- Markdown 技能系统和基础工具：文件读写、Python 执行、Shell 执行、联网搜索等。
- SQLite 会话历史、RAG、记忆系统、Web UI、桌面 GUI、终端 SSE 日志和 Shell 审批。

本次实现优先复用上述能力，补齐考核关注的可靠性、结构化状态、Trace、评测和对照实验。

## 3. 实施内容

### 3.1 公共 Agent Runtime

- 新增 `app/agent/runtime.py`，提供运行预算、计划规范化、DAG 校验、Session 隔离 ID、Trace 持久化等基础能力。
- 每次 Agent 调用生成独立 `run_id`，并与 `session_id` 绑定，避免不同会话状态混杂。
- Agent 状态增加 `started_at`、`step_count`、`stop_reason`、`trace_enabled` 等字段。
- 运行时从配置读取：
  - `agent_max_steps`
  - `agent_max_runtime_seconds`
  - `agent_max_tokens`
  - `planner_enabled`
  - `planner_parallelism`
  - `trace_enabled`
  - `eval_mode`
- 执行过程记录结构化 Trace，包括运行开始、规划生成、技能选择、工具请求、模型观察、反思结果、最终输出和运行指标。
- 前端首页展示状态、步数预算、运行时间预算、Token 预算和子任务完成情况。

### 3.2 Planner-Executor 专项

- Planner 输出升级为结构化计划：
  - `plan_id`
  - `version`
  - `goal`
  - `complexity`
  - `subtasks`
  - `ready_subtasks`
  - `replan_from`
- 子任务结构包含：
  - `id`
  - `desc`
  - `expected_result`
  - `skill`
  - `depends_on`
  - `status`
  - `result`
  - `error`
  - `retry_count`
- 实现计划合法性检查：
  - 子任务 ID 不能为空。
  - 子任务 ID 不能重复。
  - 依赖目标必须存在。
  - 禁止自依赖。
  - 检测循环依赖。
- Executor 根据已完成依赖选择 ready 子任务执行。
- Reflection 更新子任务状态，并在需要调整时触发重新规划。
- 重新规划时保留已完成子任务结果，替换不可继续的部分，形成新的计划版本。

### 3.3 Trace 与回放

- Trace 持久化到 `data/agent_trace.db`。
- 每个 run 同步生成 JSONL 文件到 `data/agent_traces/`。
- 新增后端接口：
  - `GET /agent/runs`
  - `GET /agent/runs?session_id=...`
  - `GET /agent/trace/{run_id}`
  - `GET /agent/plans/{run_id}`
- 前端可继续使用现有 `/console/stream` 查看实时日志；考核展示可使用新增 Trace 接口回放结构化执行过程。

### 3.4 自动评测与对照实验

- 新增 `data/eval/agent_eval_tasks.json`，包含 22 个评测任务。
- 任务覆盖：
  - 文件读取
  - 文件写入
  - Python 执行
  - Shell 审批
  - 多步骤规划
  - 依赖调度
  - 失败恢复
  - Trace 回放
  - 工具选择
  - 预算控制
  - 安全说明
  - 最终报告素材
- 新增 `scripts/run_agent_eval.py`：
  - 默认 dry-run，验证评测集和输出报告结构。
  - 使用 `--execute` 时实际调用 Agent。
  - 使用 `--mode planner` 或 `--mode baseline` 标注对照实验模式。
  - 输出 `data/eval/latest_results.json` 和 `data/eval/latest_results.csv`。

### 3.5 单元测试

- 新增 `tests/test_agent_runtime.py`，覆盖：
  - 计划规范化。
  - DAG 循环依赖检测。
  - 子任务 ready 调度。
  - TraceStore 写入和读取。

## 4. 验收方式

### 4.1 静态检查

```bash
uv run python -m compileall app web scripts tests
```

### 4.2 单元测试

```bash
uv run python -m unittest tests.test_agent_runtime
```

### 4.3 评测任务 dry-run

```bash
uv run python scripts/run_agent_eval.py
```

### 4.4 实际 Agent 评测

需要配置可用模型和 API Key 后执行：

```bash
uv run python scripts/run_agent_eval.py --mode planner --execute
uv run python scripts/run_agent_eval.py --mode baseline --execute
```

### 4.5 Web 手工验收

```bash
uv run python -m web.web_py
```

浏览器访问：

- `http://127.0.0.1:8000/web/html/index.html`
- `http://127.0.0.1:8000/agent/runs`
- `http://127.0.0.1:8000/agent/trace/{run_id}`

## 5. 最终 PDF 建议结构

1. 项目背景和问题定义。
2. 公共 Agent Runtime 架构。
3. Planner-Executor 数据结构和执行流程。
4. 结构化 Trace 与计划版本回放。
5. 自动评测任务、评价指标和对照实验。
6. 成功案例、失败恢复案例和错误案例。
7. 当前局限与后续改进。

## 6. 实现约束

- 保留现有 Web UI、桌面 GUI 和 `/chat` 接口兼容性。
- 不覆盖已有用户改动：`app/llm/prompts/chat_prompt.txt`、`config/personality_presets.json`、`.agents/`、启动脚本等。
- 不把 MCP 作为主专项；Markdown 技能系统作为工具系统能力展示。
- Memory 和 Sandbox 做支撑增强，不扩展为本次主方向。
