# 系统操作工具包实现记录

## 背景

项目原有 Agent 已经具备 LangGraph 编排、技能匹配、文件读写、Python 执行、Shell 执行和联网搜索能力。但本机系统操作主要依赖 `execute_shell_command`，模型需要临时生成命令，容易出现命令不兼容、输出难判断、误操作风险偏高等问题。

本次改动的目标是把常见电脑操作拆成稳定的结构化工具，让 Agent 在桌面助手场景中更容易完成文件、目录、进程、系统信息和应用启动类任务。

## 改动内容

### 1. 新增结构化系统工具

在 `app/agent/tools.py` 中新增以下工具：

- `list_directory`：列出目录内容，返回类型、大小、修改时间和名称。
- `search_files`：按文件名模式或关键词搜索文件和目录。
- `get_file_info`：查看文件或目录的路径、类型、大小、时间和读写权限。
- `copy_file`：复制文件，默认不覆盖已存在目标。
- `move_file`：移动或重命名文件/目录，默认不覆盖已存在目标。
- `make_directory`：创建目录。
- `open_path`：用系统默认程序打开文件或目录。
- `open_url`：用默认浏览器打开网址。
- `list_processes`：列出当前进程，支持按名称过滤。
- `start_app`：启动应用或命令。
- `get_system_info`：返回操作系统、Python、机器架构和当前目录信息。
- `get_disk_usage`：返回指定路径所在磁盘的容量、已用空间和可用空间。

这些工具已经加入 `DEFAULT_TOOLS`，Agent 执行节点绑定工具时会自动获得这些能力。

### 2. 改进 Shell 工具

`execute_shell_command` 保留为兜底工具，并做了两点增强：

- 增加危险命令拦截，拒绝明显高风险命令，例如 `rm -rf`、`format`、`shutdown`、`reg delete`、递归强制删除等。
- 输出改为结构化格式：

```text
exit_code: 0
stdout:
...

stderr:
...
```

这样 Agent 能更明确地区分命令成功、标准输出和错误输出。

### 3. 新增系统操作技能

新增 `app/agent/skills/system_operator.md`，用于引导 Agent 在以下场景中优先使用结构化工具：

- 打开文件、目录、软件、网址
- 查看目录、列出文件、查找文件
- 复制、移动、创建目录
- 查看进程、系统信息、磁盘空间

该技能明确要求：优先用结构化工具，只有缺少合适工具时才退回 Shell。

### 4. 新增测试

新增 `tests/test_agent_system_tools.py`，覆盖：

- 目录列出
- 文件搜索
- 文件信息读取
- 复制文件
- 移动文件
- 创建目录
- 系统信息
- 磁盘空间
- Shell 危险命令拦截

## 后续建议

下一步可以继续做三类增强：

1. 增加更细的权限策略，例如区分只读操作、写入操作、启动应用操作和高风险操作。
2. 给 `open_path`、`open_url`、`start_app` 接入前端确认弹窗。
3. 增加截图、窗口枚举、鼠标键盘自动化等 GUI 层工具，把“系统操作”扩展成真正的桌面控制能力。
